import { ITheme } from "../ITheme";

const defaultTheme: ITheme = {
  background: "#425563",
  primaryColor: "#425563",
  secondaryColor: "#425563",
  textColor: "#fcfcfc",
};

export default defaultTheme;
